apiVersion: apps/v1
kind: Deployment
metadata:
  name: caring-authority-server-deployment
spec:
  selector:
    matchLabels:
      app: caring-authority-server
  replicas: 1
  template:
    metadata:
      labels:
        app: caring-authority-server
    spec:
      containers:
        - name: caring-authority-server
          image: YOUR_REGISTRY/caring-authority-server:c.2.5.1-SNAPSHOT
          imagePullPolicy: Always
          ports:
            - name: http-port
              containerPort: 8764
      imagePullSecrets:
        - name: YOUR_REGISTRY_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: caring-authority-server
spec:
  selector:
    app: caring-authority-server
  #    env: prod
  ports:
    - protocol: TCP
      port: 8764
      targetPort: 8764